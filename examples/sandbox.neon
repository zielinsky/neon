data Flag =
  | On  (n:Int)
  | Off (n:Int)


let flagTy (f: Flag) = 
    match f with
    | On(n)   -> Int
    | Off(n)  -> Bool


let choose (f: Flag) = 
    match f with
    | On(n)   -> 0
    | Off(n)  -> 15 